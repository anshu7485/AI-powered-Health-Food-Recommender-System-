document.getElementById('loginForm').addEventListener('submit', function(e) {
    // Basic validation can be added here
    // e.g., check if fields are not empty
    // For now, let the form submit
});
